# LLM / agent authoring reference

The authoring contract for AI tools and agents that emit DataDoom specs:
the complete capability surface, the hard validation rules, and validated
few-shot examples. Embedded verbatim from the authoritative source.

!!! tip "Live manifest"
    For a machine-readable, always-current capability list (including any
    installed plugins), run `datadoom spec-reference` or
    `GET /api/spec-reference` instead of hard-coding this page.

{%
   include-markdown "../docs_v2/21_LLM_Spec_Authoring_Reference.md"
   heading-offset=1
   rewrite-relative-urls=false
%}
